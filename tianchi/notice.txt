先把负例归一化之后，再聚类，这样每条数据除了原始的label之外，再多了一个聚类的label，正例为0，其次依次递增
然后把part1的数据正负例混到一起写到一个文件中
在生成训练集的时候，从正例中按之前的比例抽取数据，再在每个cluster中按frac_ratio = sub_ratio * np_ratio / 1200
的比例抽取数据，使正负例比例大致为1。拼接起来，组成了训练集，再用之前的part_1_scaler进行归一化操作
未带标签的测试数据part3，先按特征工程的步骤生成特征数据，再训练一个scaler进行归一化