关于时间窗口的操作获取训练集的问题：这里主要针对回归问题。分类问题由于target有多个值，比如，0还是1，而
    回归问题target只有一个值。
1.采用JData中的方法，传入起始和结束时间来获取时间窗口内的特征。这种方法灵活。而且不用把所有的数据都拼接到
    一起。把时间窗口的特征统计出来后和其他的特征用主键关联即可。这种是分类问题的特征构造方式
2.采用销量预测中example2的方式。主要在构建时间窗口特征时，先定义X的起始和结束时间，然后定义Y的起始和结束时间
    在每条数据中都加上这个标志。但是这个文件中的方法好像没有窗口内的统计特征
3.采用pandas中的shift和rolling函数来构建窗口内的统计特征。类似AI蜗牛车公众号中的案例。除了正常的窗口特征，
    还构建了商品和销量之间的特征，比如均值。因为在日志中，每条记录是有item和shop的字段的。
    还构建了趋势特征。